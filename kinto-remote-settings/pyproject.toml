[project]
name = "kinto-remote-settings"
version = "0.0.0"
description = "Remote Settings plugin for Kinto"
requires-python = ">=3.12"
dependencies = [
    "kinto",
    "canonicaljson-rs",
    "ecdsa",
    "requests-hawk",
]
